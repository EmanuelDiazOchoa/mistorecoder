// cart.service.ts — placeholder
import type { Product } from '../types/models';

export const addToCart = async (product: Product) => {
  // TODO: implement
  return Promise.resolve({ success: true });
};
