// products.service.ts — placeholder
import type { Product } from '../types/models';

export const fetchProducts = async (): Promise<Product[]> => {
  // TODO: implement API call
  return Promise.resolve([]);
};
